import React from 'react';
import { SettingsCenterView } from '@/components/settings/settings-center-view';

export const revalidate = 0;

export default async function SettingsPage() {
  return <SettingsCenterView />;
}

