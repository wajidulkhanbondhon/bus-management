import React from 'react';
import { BackupConsoleClient } from '@/components/dashboard/backup-console-client';

export const revalidate = 0;

export default function BackupConsolePage() {
  return <BackupConsoleClient />;
}
