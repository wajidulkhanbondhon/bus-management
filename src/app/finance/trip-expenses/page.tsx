import React from 'react';
import { TripExpenseTrackerClient } from '@/components/finance/trip-expense-tracker-client';

export const revalidate = 0;

export default function TripExpensesPage() {
  return <TripExpenseTrackerClient />;
}
